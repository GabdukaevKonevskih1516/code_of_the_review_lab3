# Задание 1
Напишите генератор паролей. Составьте три уровня сложности генерации паролей (вместе 
с их длиной) и спрашивайте у пользователя, какой уровень сложности ему нужен. Проявите свою 
изобретательность: надёжные пароли должны состоять из сочетания строчных букв, прописных 
букв, цифр и символов. Пароли должны генерироваться случайным образом каждый раз, когда 
пользователь запрашивает новый пароль

# Задание 2
В текстовом файле с именем FN1 дано арифметическое выражение в инфиксной 
форме. В выражении могут использоваться операции: сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), а так же целые числа из про
межутка [1; 30] и переменная x. Для операции возведения в степень показатель степени неотри
цательное целое число. Постройте дерево выражения. После этого вычислите значение выраже
ния при заданном значении переменной x и выведите результат в текстовый файл с именем FN2. 
Преобразуйте дерево, заменив все поддеревья вида x*A на A*x, где A - некоторое поддерево, а x - переменная. Распечатайте дерево после преобразования в файл FN2 используя многострочный 
формат, в котором дерево положено на бок. Каждый уровень дерева выводите в 4-х позициях и 
используйте выравнивание по правому краю. При наличии нескольких подряд идущих одинако
вых операций дерево должно строиться по правилу: операции одинакового приоритета вычисля
ются по порядку слева направо. Иными словами, выражение 2+3+4+5, например, должно тракто
ваться как ((2+3)+4)+5, и не может трактоваться как (2+3)+(4+5) или 2+(3+(4+5)). Результаты всех 
вычислений, включая промежуточные, принадлежат типу int. 

# Задание 3
Дано описание неориентированного графа в текстовом файле с именем FileName. в виде 
матрицы смежности. Первая строка файла содержит количество вершин графа (n), а следующие 
n строк содержат матрицу смежности (m), m[i][j]=0, если ребра между вершинами i и j не суще
ствует. Определить степень для каждой вершины графа. Вывести степени вершин, перечисляя их 
в порядке возрастания номеров вершин. Если в графе имеются петли, то каждая петля в степени 
вершины учитывается дважды.

# ОТЧЕТ ПО ЗАДАНИЯМ

* Всем нейронкам был дан один и тот же промт для выполнения всех заданий:

Реши эту задачу на С++, важные условия для решения:

1) Стиль: Строго следуй кодстайлу Epic Games Coding Standard.

2) Архитектура: Раздели результат на 3 файла:  main.cpp, Source.cpp, Header.h.

3) Безопасность: Добавь защиту от неверного ввода, грубо говоря нужно сделать защиту от дурака.

4) Также если в задаче есть файл, приведи также пример файла.

* Депсек: отлично справился с поставленной задачей, когда я предлагал что-то добавить в код (вне задания), реагировал нормально, под запросы подставивалс, довольно чисто отработал, без каких либо критических изменений, приводил в примеры файлы с вводными данными, приятный дружественный интерфейс, из минусов это большой по объему код, намного больше чем в том же джимини. (https://gemini.google.com/?hl=ru)

* Джимини: прекрасная нейронка, очень хорошо выполнила задачу, приятный и четко понимаемый по структуре код, без лишней воды интерфейс, приводил файлы с водными данными, лучше всех выполнил поставленную задачу. (https://chat.deepseek.com/)

* Алиса AI: неплохая нейросеть, но до джимини и до депсека очень далеко, были ошибки во 2 задаче, прога не правильно считала вершины графа, очень большие коды, постоянно зависала и выдавала коды отрывками, максимально не удобный интерфейс у проги и у самой нейронки, плюс нет возможности поделиться чатом с другими пользователями. (https://alice.yandex.ru/)






